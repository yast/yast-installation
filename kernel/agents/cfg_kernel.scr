/**
 * File:
 *   cfg_kernel.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/sysconfig/kernel
 *   using the sysconfig-agent
 * 
 * $Id$
 *
 * Read/Sets the values defined in <tt>/etc/sysconfig/kernel</tt>
 * in an easy manner.
 */
.sysconfig.kernel

`ag_ini(
  SysConfigFile("/etc/sysconfig/kernel")
)
